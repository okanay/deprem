import Link from "next/link";
import { pages } from "./layoutData/pagesData";

import { useRouter } from "next/router";

const MobileNavBar = () => {
  const { pathname } = useRouter();

  return <div></div>;
};

export default MobileNavBar;
